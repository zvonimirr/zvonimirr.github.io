---
const links = [
  {
    label: "Projects",
    url: "/projects",
  },
  {
    label: "About me",
    url: "/about",
  },
  {
    label: "Contact",
    url: "/contact",
  },
];
---
<nav>
  <ul class="flex flex-row gap-2 justify-end">
    {links.map((link) => {
      return (
        <li class="font-robotoCondensed hover:underline">
          <a href={link.url}>{link.label}</a>
        </li>
      );
    })}
  </ul>
</nav>
