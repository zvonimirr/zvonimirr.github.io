---
import { cn } from "../utils/cn";
import { Icon } from "astro-icon";

const id = Astro.props.id;
const title = Astro.props.title;
const name = Astro.props.name;
---

<abbr title={title}>
  <Icon
    name={name}
    class={cn("w-16", "h-16", Astro.props.class)}
    data-tech={id}
  />
</abbr>
